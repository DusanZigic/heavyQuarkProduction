params = {
    "sNN":    ["200GeV", "5020GeV"],    # collision energies
                           # options: 200GeV, 2760GeV, 5020GeV, 5440GeV or any combination (use list of strings for multiple options)
                           # if left empty, pT distributions will be generated for all collision energies
    
    "quarks": ["charm", "bottom"],      # heavy quark types
                            # options: charm, bottom or both (use list of strings for multiple options)
                            # if left empty, pT distributions will be generated for all heavy quarks

    }